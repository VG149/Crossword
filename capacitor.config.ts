import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seunome.palavrascruzadas',
  appName: 'Palavras Cruzadas',
  webDir: 'dist'
};

export default config;
